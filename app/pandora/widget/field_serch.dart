import 'package:flutter/material.dart';

class FieldSerch extends StatelessWidget {
  const FieldSerch({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
      height: 100,
    );
  }
}
